@component('mail::message')
{{-- Greeting --}}
@if (! empty($greeting))
# {{ $greeting }}
@else
@if ($level == 'error')
# Whoops!
@else
# Hallo!
@endif
@endif

{{-- Intro Lines --}}
@foreach ($introLines as $line)
{{ $line }}

@endforeach

{{-- Action Button --}}
@isset($actionText)
<?php
    switch ($level) {
        case 'success':
            $color = 'green';
            break;
        case 'error':
            $color = 'red';
            break;
        default:
            $color = 'blue';
    }
?>
@component('mail::button', ['url' => $actionUrl, 'color' => $color])
{{ $actionText }}
@endcomponent
@endisset

{{-- Outro Lines --}}
@foreach ($outroLines as $line)
{{ $line }}

@endforeach

{{-- Salutation --}}
@if (! empty($salutation))
{{ $salutation }}
@else
Vriendelijke groeten,<br>Beeldr
@endif

{{-- Subcopy --}}
@isset($actionText)
@component('mail::subcopy')
Als u een probleem hebt klik op de "{{ $actionText }}" knop, kopieer en plak de onderstaande URL
in uw webbrowser:[{{ $actionUrl }}]({{ $actionUrl }})
@endcomponent
@endisset
@endcomponent
